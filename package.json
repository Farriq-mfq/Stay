{
  "name": "stay",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "start": "turbo start",
    "lint": "turbo lint",
    "start:prod": "turbo start:prod",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dep": "node scripts/deploy.js",
    "setapp": "node scripts/setup.js"
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
